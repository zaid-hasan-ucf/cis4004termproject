{
  "name": "cis4004termproject",
  "workspaces": ["server","client"],
  "scripts": {
    "setup": "node setup.js",
    "setup:fresh": "node setup.js --seed",
    "seed": "npm --prefix server run seed",
    "db": "mongod --config mongod.cfg",
    "dev": "concurrently \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "dev:all": "concurrently \"npm run db\" \"npm --prefix server run dev\" \"npm --prefix client run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
